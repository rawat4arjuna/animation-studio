// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://neondb_owner:npg_vTrp61FEliqc@ep-broad-glitter-ahqfovmu-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
}

model Project {
  id          String   @id @default(cuid())
  name        String
  description String?
  thumbnail   String?
  frameCount  Int      @default(0)
  fps         Int      @default(12)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  frames      Frame[]
}

model Frame {
  id         String   @id @default(cuid())
  projectId   String
  frameIndex  Int
  imageData   String   // Base64 encoded image data
  thumbnail   String   // Base64 encoded thumbnail
  createdAt   DateTime @default(now())
  
  project     Project @relation(fields: [projectId], references: [id], onDelete: Cascade)
  
  @@unique([projectId, frameIndex])
}